import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.demo.app',
  appName: 'appflow monorepo demo',
  webDir: 'dist',
  bundledWebRuntime: false,
}

export default config;